# Headless browser for the UI walkthrough (dev only; users never need this).
FROM mcr.microsoft.com/playwright/python:v1.52.0-noble
RUN pip install --no-cache-dir --break-system-packages playwright==1.52.0
